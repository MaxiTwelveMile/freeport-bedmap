# Copy this file to .env for local development
# On Railway, set these as environment variables in the dashboard

# Password that all staff use to log in
APP_PASSWORD=changeme

# Set automatically by Railway when you add a Postgres database — do not set manually
DATABASE_URL=
